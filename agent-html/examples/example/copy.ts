export const exampleCopy = {
  title: "Use the entry file as the map, not the whole artifact.",
  summary:
    "Broad artifacts stay readable when the discoverable *.artifact.tsx file owns sequence and each semantic block owns its local implementation.",
  alert:
    "This example is intentionally split. Copy this shape when an artifact grows past a compact static or instrumented example.",
  prompt:
    "Rewrite one block in this artifact. Keep example.artifact.tsx as the artifact entry and make the target block own its local UI, copy, and state.",
}
