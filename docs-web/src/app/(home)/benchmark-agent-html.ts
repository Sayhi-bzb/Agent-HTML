import { benchmarkCopy } from "./benchmark-copy"

export const agentHtmlSource = [
  '<meta-agent profile-ref="review-dense" />',
  "",
  `<page title="${benchmarkCopy.title}">`,
  `  <alert title="Status" variant="default">${benchmarkCopy.summaryStatus}</alert>`,
  "",
  '  <card title="Summary">',
  `    ${benchmarkCopy.summary}`,
  "  </card>",
  "",
  `  <tabs>`,
  `    <tab value="summary" label="${benchmarkCopy.tabNames.summary}">`,
  '      <card title="Release checklist">',
  '        <list variant="unordered">',
  ...benchmarkCopy.checklist.map((item) => `          <item>${item}</item>`),
  "        </list>",
  "      </card>",
  `      <alert title="Recommendation" variant="default">${benchmarkCopy.recommendation}</alert>`,
  "    </tab>",
  "",
  `    <tab value="rollout" label="${benchmarkCopy.tabNames.rollout}">`,
  '      <card title="Rollout steps">',
  '        <list variant="ordered">',
  ...benchmarkCopy.rolloutSteps.map((item) => `          <item>${item}</item>`),
  "        </list>",
  "      </card>",
  "      <table>",
  "        <row>",
  "          <cell>Evidence</cell>",
  "          <cell>Status</cell>",
  "        </row>",
  ...benchmarkCopy.evidence.map(
    ([label, value]) => [
      "        <row>",
      `          <cell>${label}</cell>`,
      `          <cell>${value}</cell>`,
      "        </row>",
    ].join("\n"),
  ),
  "      </table>",
  "    </tab>",
  "",
  `    <tab value="risks" label="${benchmarkCopy.tabNames.risks}">`,
  "      <accordion>",
  ...benchmarkCopy.risks.map(
    (risk, index) => [
      `        <accordion-item value="risk-${index + 1}" title="${risk.title}">`,
      `          ${risk.body}`,
      "        </accordion-item>",
    ].join("\n"),
  ),
  "      </accordion>",
  "    </tab>",
  "  </tabs>",
  "</page>",
].join("\n")
