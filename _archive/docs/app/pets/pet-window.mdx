---
title: Pet Host
description: How pet companion behavior is hosted inside the main workspace frame.
---

Pet companion behavior is hosted inside the main App window.

## Host

`WorkspacePetHost` owns the visible in-workspace pet surface. It consumes App
state directly instead of synchronizing through a separate native window.

## State

Pet host state includes enablement, presence, prompt draft state, and thread
picker state.
