---
title: Workspace Pet
description: How pets appear and behave around the main workspace.
---

The workspace pet is the in-workspace companion surface.

Current ghost actions are:

- `message`
- `threads`

The radial menu uses tooltips and keeps action buttons local to the pet surface.
