---
title: Schema
description: How the Agent-HTML runtime defines allowed DSL structure.
---

Schema owns the allowed Agent-HTML vocabulary.

The authoritative source is `agentHtmlComponentRegistry`.

## Schema Owns

- tags and attributes
- component contracts
- component registry
- defaults
- validation rules
- prompt grammar support

If a change affects what agents may author, change Schema first.
