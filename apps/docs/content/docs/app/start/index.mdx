---
title: Start
description: Learn what the Agent-HTML app is for and how the product surface is organized.
---

The Agent-HTML app is the human-facing workspace for AI-native artifacts. It is
where generated work becomes something you can preview, review, compare, and
continue without losing the shape of the result inside a chat transcript.

The app has one shell and two surface modes:

- **Workspace** is the default mode. It owns projects, sections, open document
  tabs, and unsaved-change guards.
- **Gallery** is the design mode. It owns Theme, Components, and Pets gallery
  views.

Pets are app companions around workspace activity. Runtime source, schema, and
rendering contracts live under Runtime.

## When to Use App Docs

Use this section when you want to:

- understand the Agent-HTML product surface
- review or navigate generated artifacts
- work across projects and open contexts
- inspect gallery scenes and design studies
- control visible companion behavior

Module ownership lives in the project architecture doc.
