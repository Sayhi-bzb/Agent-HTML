---
title: Rendering Flow
description: The standard source-to-rendered-output pipeline for Agent-HTML.
---

Pipeline:

```text
source string
  -> parseAgentHtml
  -> validateAgentHtml
  -> renderAgentHtml
  -> runtime UI
```

Static rendering gives each explicit `Block` a stable content frame. Interactive
rendering adds wrappers only for explicit `Block` interaction units.
