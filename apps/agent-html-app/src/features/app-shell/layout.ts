export const shellPanelConstraints = {
  sessions: {
    defaultSize: 18,
    minSize: 16,
  },
  workbench: {
    defaultSize: 56,
    minSize: 38,
  },
  shell: {
    defaultSize: 26,
    minSize: 22,
  },
} as const
