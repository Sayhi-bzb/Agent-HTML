export const shellPanelConstraints = {
  sessions: {
    defaultSize: 17,
    minSize: 16,
  },
  workbench: {
    defaultSize: 60,
    minSize: 40,
  },
  shell: {
    defaultSize: 23,
    minSize: 20,
  },
} as const
